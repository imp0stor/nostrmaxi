## Analytics Premium Gate

When analytics API returns 403 with `error: 'premium_required'`:

1. Show upgrade prompt instead of analytics
2. Display: "Analytics requires a NostrMaxi identity"
3. CTA button: "Get Your NIP-05" -> /get-nip05
4. Show preview/teaser of what analytics offers (blurred charts?)
